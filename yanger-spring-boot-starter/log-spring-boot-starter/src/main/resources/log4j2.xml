<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="OFF" monitorInterval="10">
    <Properties>
        <Property name="PID">????</Property>
        <Property name="LOG_EXCEPTION_CONVERSION_WORD">%xwEx</Property>
        <Property name="LOG_LEVEL_PATTERN">%5p</Property>
        <property name="LOG_CHARSET">UTF-8</property>
        <!--@formatter:off-->
        <property name="CONSOLE_LOG_PATTERN">%clr{%d{yyyy.MM.dd HH:mm:ss.SSS}}{faint} %clr{[${LOG_LEVEL_PATTERN}]} [%appType] [%traceId] %clr{${sys:PID}}{magenta} %clr{-}{faint} %clr{[%15.15t]}{faint} %clr{%-40.40c{1.}}{cyan} %clr{:}{faint} %m%n${sys:LOG_EXCEPTION_CONVERSION_WORD}</property>
        <!--@formatter:on-->
    </Properties>

    <Appenders>
        <Console name="CONSOLE" target="SYSTEM_OUT">
            <PatternLayout charset="${LOG_CHARSET}">
                <MarkerPatternSelector defaultPattern="${sys:CONSOLE_LOG_PATTERN}">
                    <PatternMatch key="banner" pattern="%m%n"/>
                    <PatternMatch key="properties" pattern="%m%n"/>
                    <PatternMatch key="processor" pattern="%m%n"/>
                </MarkerPatternSelector>
            </PatternLayout>
        </Console>
    </Appenders>
    <Loggers>
        <!-- other -->
        <Logger name="com.fkhwl" level="DEBUG" additivity="false" includeLocation="${sys:SHOW_LOG_LOCATION}">
            <AppenderRef ref="CONSOLE"/>
        </Logger>

        <Root level="INFO">
            <AppenderRef ref="CONSOLE"/>
        </Root>
    </Loggers>
</Configuration>
